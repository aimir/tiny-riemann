option inplace;
option consume;
option implicit_halt;

global x;
global lcm;
global l;
global num;
global denom;
global i;
global c;
global d;

proc harmonic() {
    num = 0;
    denom = 1;
    while (i > 0) {
        num = num * i + denom;
        denom = denom * i;
        i = i - 1;
    }
}

proc main() {
    lcm = 1;
    while (true) {
        x = x + 1;
l = lcm;
        i = lcm;
        while (i > 0) {
            while (i >= x) { i = i - x; }
            if (i > 0) { lcm = lcm + l; i = lcm; }
        }
        if (x > 253) {
            i = x;
            harmonic();
            c = num * num;
            d = denom * denom;
            i = lcm;
            harmonic();
            num = (num - denom * x) * d;
            denom = denom * c;
            if (num * num > denom * denom * x) {
                return;
            }
        }
    }
}
